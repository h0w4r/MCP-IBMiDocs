Join logical files in DDS

Join logical files in DDSLast Updated: 2025-09-29

Join logical files combine different fields from more than one
physical file into a single record. You must specify the JFILE keyword at
the record level for join logical files.

Specify the entries in the following order to define a join logical
file:

Note: Because only one record format is allowed in a join
logical file, specify these entries only once.

Optional: Specify file-level entries
Specify record-level entries
Specify join-level entries
Specify field-level entries
Optional: Specify key field-level entries
Optional: Specify select/omit-field level entries

The following example shows the code for a join logical file.|...+....1....+....2....+....3....+....4....+....5....+....6....+....7....+....8
00010A* Joins fields from two physical files into one record format
00020A R RECORD1 JFILE(PF1 PF2)
00030A J JOIN(PF1 PF2)
00040A JFLD(NAME NAME)
00050A NAME JREF(1)
00060A ADDR
00070A PHONE
A