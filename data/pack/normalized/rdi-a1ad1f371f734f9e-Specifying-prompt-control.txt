Specifying prompt control

IBM i programming resources > Control language > Control language > CL programming > Defining and documenting commands > Defining commands > Defining commands

IBM i programming resources > Control language > Control language > CL programming > Defining and documenting commands > Defining commands

Specifying prompt control
You can control which parameters are displayed for
a command during prompting by using prompt control specifications.
This control can simplify prompting for a command by displaying
only the parameters that you want to see.
You can specify that a parameter
be displayed depending on the value specified for other parameters. This
specification is useful when a parameter has meaning only when another parameter
(called a controlling parameter) has a certain value.
You can also specify
that a parameter be selected for prompting only if additional parameters are
requested by pressing a function key during prompting. This specification
can be used for parameters that are seldom specified by the user, either because
the default is normally used or because they control seldom-used functions.
If
you want to show all parameters for a command that has prompt control specified,
you can request that all parameters be displayed by pressing F9 during prompting.

Specifying conditional prompting
You can specify that a parameter is prompted for only when certain
conditions are met.
Additional parameters for prompting
If you want a parameter that is infrequently used
not to be prompted for unless the user requests additional parameters by pressing
a function key during prompting, specify PMTCTL(*PMTRQS) on the PARM statement
for the parameter.

Parent topic: Defining commands

Related concepts
CL command delimiters
CL command definition

Related reference
Command definition statements