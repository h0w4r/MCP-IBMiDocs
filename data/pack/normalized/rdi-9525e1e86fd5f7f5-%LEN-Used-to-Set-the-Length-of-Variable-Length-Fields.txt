%LEN Used to Set the Length of Variable-Length Fields

IBM i programming resources > ILE RPG > ILE RPG Reference > Operations, Expressions, and Functions > Built-in Functions > %LEN (Get or Set Length) > %LEN (Get or Set Length)

IBM i programming resources > ILE RPG > ILE RPG Reference > Operations, Expressions, and Functions > Built-in Functions > %LEN (Get or Set Length)

%LEN Used to Set the Length of Variable-Length Fields

When used on the left-hand side of an expression, this function
sets the current length of a variable-length field. If the set length
is greater than the current length, the characters in the field between
the old length and the new length are set to blanks.
Note: %LEN can only be used on the left-hand-side of an expression
when the parameter is variable length, and when *MAX is not specified.

For alphanumeric fields, the length always refers to the number of bytes.
For UCS-2 and graphic fields, the length always refers to the number of double bytes.

Figure 1. %LEN with Variable-Length Field Example

*..1....+....2....+....3....+....4....+....5....+....6....+....7...+....
D*Name++++++++++ETDsFrom+++To/L+++IDc.Keywords+++++++++++++++++++++++++
*
D city S 40A varying inz('North York')
D n1 S 5i 0

* %LEN used to get the current length of a variable-length field:
/FREE
n1 = %len(city);
// Current length, n1 = 10

// %LEN used to set the current length of a variable-length field:
%len (city) = 5;
// city = 'North' (length is 5)

%len (city) = 15;
// city = 'North ' (length is 15)
/END-FREE

Parent topic: %LEN (Get or Set Length)