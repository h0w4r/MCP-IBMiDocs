Command exit programs and independent ASPs

IBM i programming resources > Control language > Control language > CL programming > Defining and documenting commands > Defining commands > Command definition process > Command definition process

IBM i programming resources > Control language > Control language > CL programming > Defining and documenting commands > Defining commands > Command definition process

Command exit programs and independent ASPs
Command exit programs needed by a command cannot be in a different
independent auxiliary storage pool (ASP).
Any exit program, including the command processing program, validity checking
program, prompt override program, choices program, or prompt control program,
needed by a command must be in the same independent auxiliary storage pool
(ASP) as the command, or in the system ASP (ASP 1), or in a basic ASP (ASPs
2-32). The command must not be in one independent ASP and the exit programs
in another independent ASP. Problems could occur when running the command
if the independent ASP where these exit programs reside is not available
(for example, if the independent ASP device is varied off).

Parent topic: Command definition process