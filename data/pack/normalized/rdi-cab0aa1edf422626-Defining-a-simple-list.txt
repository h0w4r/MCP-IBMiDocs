Defining a simple list

IBM i programming resources > Control language > Control language > CL programming > Defining and documenting commands > Defining commands > Defining lists for parameters > Defining lists for parameters

IBM i programming resources > Control language > Control language > CL programming > Defining and documenting commands > Defining commands > Defining lists for parameters

Defining a simple list
A simple list can accept one or more values of the type specified
by the parameter.
For example, if the parameter is for the user name, a simple
list means that more than one user name can be specified on that parameter.
USER(JONES SMITH MILLER)
If a parameter's value is a simple list, you specify the maximum
number of elements the list can accept using the MAX parameter on the PARM
statement. For a simple list, no command definition statements other than
the PARM statement need be specified.
The following example defines
a parameter USER for which the display station user can specify up to five
user names (a simple list).
PARM KWD(USER) TYPE(*NAME) LEN(10) MIN(0) MAX(5) +
SPCVAL(*ALL) DFT(*ALL)
The parameter is an optional parameter as specified by MIN(0)
and the default value is *ALL as specified by DFT(*ALL).
When the elements
in a simple list are passed to the command processing program, the format
varies depending on whether you are using CL or another high-level language
(HLL), or REXX.

Using CL or other HLLs for simple lists
When a command is run using CL or another high-level language (HLL),
the elements in a simple list are passed to the command processing program
in this format.
Using REXX for simple lists
When the same command is run, the elements in a simple list are
passed to the REXX procedure
in the argument string in the format where valueN is the
last value in the simple list.

Parent topic: Defining lists for parameters