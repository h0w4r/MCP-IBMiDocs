THREAD(*SERIALIZE)

IBM i programming resources > ILE RPG > ILE RPG Reference > Specifications > Control Specifications > Control-Specification Keywords > THREAD(*CONCURRENT | *SERIALIZE) > THREAD(*CONCURRENT | *SERIALIZE)

IBM i programming resources > ILE RPG > ILE RPG Reference > Specifications > Control Specifications > Control-Specification Keywords > THREAD(*CONCURRENT | *SERIALIZE)

THREAD(*SERIALIZE)

If THREAD(*SERIALIZE) is specified, access to the procedures in the module
is serialized. When called in a multithreaded environment, any code within
the module can be used by at most one thread at a time.

Parent topic: THREAD(*CONCURRENT | *SERIALIZE)