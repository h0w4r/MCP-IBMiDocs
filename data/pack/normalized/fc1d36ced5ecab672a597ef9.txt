Without operators

IBM i programming resources > SQL > SQL reference > Language elements > Expressions > Expressions

IBM i programming resources > SQL > SQL reference > Language elements > Expressions

Without operators
If no operators are used, the result of the expression is the specified
value.
Example SALARY :SALARY 'SALARY' MAX(SALARY)

Parent topic: Expressions