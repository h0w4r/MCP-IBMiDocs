Constant value

IBM i programming resources > Control language > Control language > CL concepts > CL parameters > Parameter values > Parameter values

IBM i programming resources > Control language > Control language > CL concepts > CL parameters > Parameter values

Constant value
A constant value is the actual value. The types of
constants are character string (includes names, date and hexadecimal values),
decimal, and logical.
A constant value is an actual numeric value or a specific character
string whose value does not change. Three types of constants can be used by
the control language: character string (quoted or unquoted), decimal, and
logical.

Character string
A character string is a string of any EBCDIC characters
(alphanumeric and special) that are used as a value.
Hexadecimal values
A hexadecimal value is a constant
that is made up of a combination of the hexadecimal digits A through F and
0 through 9.
Date values
A date value is a character string that represents
a date.
Decimal values
A decimal value is a numeric string of one or more
digits, optionally preceded by a plus (+) or minus (-) sign.
Logical values
A logical value is a single character
(1 or 0) that is enclosed in single quotation marks.
Floating-point constants
A floating-point constant is a representation of a
numerical constant, shown as an optional sign followed by one or more digits
and a decimal point, which may be at the end.

Parent topic: Parameter values