Parameter values

IBM i programming resources > Control language > Control language > CL concepts > CL parameters > CL parameters

IBM i programming resources > Control language > Control language > CL concepts > CL parameters

Parameter values
A parameter value is user-supplied
information that is used during the running of a command.
An individual value can be specified in a constant value, a variable
name, an expression, or a list of values. A parameter can specify one or a
group of such values, depending on the parameter's definition in a command.
If a group of values is specified, the parameter is called a list parameter because
it can contain a list of values.
On commands with key and positional
parameters, values can be specified in keyword form, positional form, or a
combination of both forms. Parameter values must be enclosed in parentheses
if any of the following conditions are true:
A keyword precedes the value.
The value is an expression.
A list of values is specified. Note: If only one value is specified
for a list, no parentheses are required.

Constant value
A constant value is the actual value. The types of
constants are character string (includes names, date and hexadecimal values),
decimal, and logical.
Variable name
A variable contains a data value that can be
changed when a program is run. A variable name is the name of
the variable that contains the value.
Expressions
An expression is a group of constants or variables,
separated by operators, that produces a single value.
List of values
A list of values is one or more values that can be
specified for a parameter.

Parent topic: CL parameters

Related concepts
CL command coding rules

Related reference
CL command parameters